import {createContext} from 'react';

const ProductsContext = createContext() //empezar  a hacer el flujo de datos hacia varios componentes

export default ProductsContext

//Permitiendo que todos los componentes puedan recibir datos del sol (estado global)